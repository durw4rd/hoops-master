# Texture & Placeholder Assets

| File | Used by |
|------|---------|
| `concrete-tile.png` | `.concrete-bg` — page background |
| `brick-tile.png` | `.brick-panel` — optional utility (not used in main flows) |
| `default-crew-wall.png` | `CrewMuralHero` when crew has no banner |
| `empty-crews.png` | `GroupList` empty state |

Unused in UI (kept for future): `spray-drip.png`, `subway-tunnel.png`, `court-lines.png`, `tape-corner-*.png`
